<% include header %>


<div class="wrapper">

	<% if (!user) { %>
		<h2>Welcome! Please log in.</h2>
		<a href="/auth/google">Login with Google</a>
	<% } else { %>
		<h2>Welcome back, <%= user.displayName %>.</h2>
		<div class="parallax">
			<div class="calendar"></div>
			<div class="tasks">
				<a href="">Completed<br />Tasks</a>
			</div>
		</div>
	<% } %>

</div>


<% include footer %>
